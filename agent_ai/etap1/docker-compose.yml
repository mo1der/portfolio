name: ai-classifier

services:
  ai-classifier-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-classifier-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped